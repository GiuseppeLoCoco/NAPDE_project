from firedrake import*
from interpolation import delta_interpolation
# Delta-interpolation
def interpolate_nonmatching_mesh_delta(fsi_interpolation, u0, V, abc, flag):

    
    u = Function(V)
    u.assign(0.0)
    fsi_interpolation.interpolate_delta(u0, u, abc, flag)
    return u 